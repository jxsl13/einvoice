name: Dependency auto-merge

on:
  pull_request_target:
    types: [opened, reopened, synchronize, ready_for_review, labeled]
  workflow_dispatch:
    inputs:
      pull_request:
        description: Pull request number created by trusted dependency automation
        required: true
        type: number

permissions:
  contents: write
  pull-requests: write

jobs:
  dependency:
    name: Arm trusted dependency auto-merge
    if: >-
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.user.login == 'dependabot[bot]' ||
      github.event.pull_request.user.login == 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout protected policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false

      - name: Verify dependency provenance and enable protected squash merge
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number || inputs.pull_request }}
        shell: bash
        run: |
          set -euo pipefail
          details="$(gh pr view "${PR_NUMBER}" --repo "${GITHUB_REPOSITORY}" \
            --json author,files,isDraft,labels)"
          if ! jq -e -f .github/release/dependency-automerge.jq \
            <<<"${details}" >/dev/null; then
            echo 'Pull request is not an allowlisted automated dependency update.' >&2
            exit 1
          fi
          gh pr merge "${PR_NUMBER}" --repo "${GITHUB_REPOSITORY}" --auto --squash
